/**
 * Client compatibility entry point for the shared Factory access registry.
 * Backend and frontend authorization must use one canonical page map.
 */
export * from "@shared/factoryAccessRegistry";
